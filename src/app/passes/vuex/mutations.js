export const setPasses = (state, passes) => {
    state.passes = passes
}

export const setPass = (state, pass) => {
    state.pass = pass
}

export const setMeta = (state, meta) => {
    state.meta = meta
}
